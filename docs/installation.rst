============
Installation
============

At the command line::

    $ easy_install pylearnutils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pylearnutils
    $ pip install pylearnutils